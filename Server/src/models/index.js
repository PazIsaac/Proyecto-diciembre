const User = require('./User');
const Product = require('./Product');

// Definir asociaciones aquí si las necesitas en el futuro
// Por ejemplo: User.hasMany(Orders), etc.

module.exports = {
    User,
    Product
};